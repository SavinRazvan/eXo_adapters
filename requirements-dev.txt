# Minimal dev tools only. For pytest + imports against packages, use:
#   pip install -r requirements-ci.txt
#
# Build + publish (local): pip install -r requirements-release.txt
pytest>=9.0.0
pytest-asyncio>=1.3.0
pytest-cov>=7.0.0
build>=1.2.0
pip-audit>=2.7.0
