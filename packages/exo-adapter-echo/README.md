# exo-adapter-echo

Portable echo `RuntimeAdapter` (no network) for conformance tests and multi-adapter parity checks.

Part of [eXo_adapters](https://github.com/SavinRazvan/eXo_adapters). Requires `exo-brain-core-contracts`.
